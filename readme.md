# Simple Addition Script

This repository contains a simple Python script that performs the addition of two numbers.

## Description
The `add_numbers.py` file defines a function called `add_numbers` which takes two numeric values as input and returns their sum.  
An example usage is included in the script to demonstrate how the function works.

## How to Run
Make sure you have Python installed, then run:

```bash
python add_numbers.py
